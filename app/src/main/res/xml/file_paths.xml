<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Cache directory para fotos temporales -->
    <cache-path
        name="images"
        path="/" />

    <!-- External cache directory (opcional) -->
    <external-cache-path
        name="external_images"
        path="/" />

    <!-- Files directory (opcional) -->
    <files-path
        name="files"
        path="/" />
</paths>